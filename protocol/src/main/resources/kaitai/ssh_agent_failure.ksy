meta:
  id: ssh_agent_failure
  title: SSH_AGENT_FAILURE
  endian: be
doc: |
  Generic failure response (type 5).
  Sent when a request cannot be fulfilled (denied signing, unknown extension, etc.).
  Has no payload beyond the message type.
seq: []
